'use strict';

var React = require('react');

var Verbs = React.createClass({
    render: function() {
        return (
            <div>
                Hi From Verbs
            </div>
        )
    }
})

module.exports = Verbs;