'use strict';

var React = require('react');

var PhrasesStats = React.createClass({
    render: function() {
        return (
            <div>
                You're in STATS now
            </div>
        )
    }
})

module.exports = PhrasesStats;