'use strict';

var React = require('react');

var Verbs = React.createClass({
    render: function() {
        return (
            <div>
                Try translating this verb:
            </div>
        )
    }
})

module.exports = Verbs;